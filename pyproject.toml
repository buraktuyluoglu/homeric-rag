[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "homeric-rag"
version = "0.1.0"
description = "Entity-aware hybrid retrieval over Homer's Iliad and Odyssey (Samuel Butler prose translations)"
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Burak Emre Tüylüoğlu" }]
requires-python = ">=3.12"
# Bounded so a future resolve stays near the versions the committed run
# used (recorded in out/results.json under "library_versions").
dependencies = [
    "onnxruntime>=1.17,<2",
    "transformers>=4.40,<6",
    "numpy>=1.26,<3",
    "huggingface_hub>=0.23,<2",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]

[project.scripts]
homeric-rag = "homeric_rag.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
